[package]
name = "backend"
version = "0.5.1"
edition = "2021"

[dependencies]
rocket = {version = "0.5.1", features = ["json"]}
rocket_cors = "0.6.0"
sqlite = "*"
domino-lib = "1.0.0"
rand = "0.8.5"
serde = "*"
